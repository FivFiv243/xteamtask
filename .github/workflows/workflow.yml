name: Flutter PR

on:
 pull_request:
   branches:
     - "origin/main"


jobs:
 build:
   runs-on: ubuntu-latest
   steps:
    - uses: actions/checkout@v2
    - run: flutter pub get
    - run: flutter build apk --release